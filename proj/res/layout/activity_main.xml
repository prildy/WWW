<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >
	
    <LinearLayout
        android:id="@+id/what_box"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:orientation="horizontal">
		    <EditText
		        android:id="@+id/whatEditText"
		        android:layout_width="0dp"
		        android:layout_height="match_parent"
		        android:hint="@string/what"
		        android:layout_weight="1"/>
	</LinearLayout>
	<LinearLayout
        android:id="@+id/where_box"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_below="@id/what_box"
        android:orientation="horizontal">
		    <EditText 
		        android:id="@+id/whereEditText"
		        android:layout_width="0dp"
		        android:layout_height="match_parent"
		        android:hint="@string/where"
		        android:layout_weight="1"/>
	</LinearLayout>
	<LinearLayout
	    android:layout_below="@id/where_box"
        android:id="@+id/when_box"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:orientation="horizontal">
		    <EditText
		        android:id="@+id/when_button"
		        android:focusable="false"
		        android:focusableInTouchMode="false"
		        android:editable="false" 
		        android:layout_width="0dp"
		        android:layout_height="match_parent"
		        android:hint="@string/when"
		        android:layout_weight="1"/>
	</LinearLayout>
	<Button 
	    android:id="@+id/go_button"
	    android:layout_centerHorizontal="true"
	    android:layout_below="@id/when_box"
	    android:layout_width="match_parent"
	    android:layout_height="wrap_content"
	    android:textSize="12dp"
	    android:text="Find"/>
</RelativeLayout>
